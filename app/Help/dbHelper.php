<?php

//define

//functions
/* if (!function_exists('getSettings')) {
    function getSettings($name = 'title')
    {
        $setting = Setting::where('name', $name)->first();

        return $setting == null ? $name : $setting->values;
    }
}
 */
